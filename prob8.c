#include <stdio.h> 

int main(){
    int a;
    float b;
    double c;
    char ch;
    printf("the size of int is %d\n",sizeof(a));
    printf("the size of int is %d\n",sizeof(b));
    printf("the size of int is %d\n",sizeof(c));
    printf("the size of int is %d",sizeof(ch));
    return 0;
}